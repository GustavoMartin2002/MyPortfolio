import ServicesList from "./ServicesList";

export default function AllServices() {
  return (
    <section className="min-h-screen w-[80%] m-auto flex justify-center items-start max-sm:w-[90%]">
      <ServicesList/>
    </section>
  );
}